# Frontend (React)

Simple React app with two pages: Profile and Dashboard. Uses `axios` to call the backend API.

Run:

```powershell
cd frontend
npm install
npm start
```

By default the frontend expects the backend at `http://localhost:5000`. Set `REACT_APP_API_URL` to change.
